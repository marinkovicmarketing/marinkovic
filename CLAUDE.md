# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository (`marinkovicmarketing/marinkovic`) is currently empty — it has no commits, no branches, and no files. There is no codebase, build system, or test suite to document yet.

When code is added to this repository, update this file to include:

- **Development commands**: how to install dependencies, build, lint, run tests (including running a single test), and run the project locally.
- **Architecture**: the high-level structure of the codebase — major modules/services, how they interact, and any non-obvious design decisions that span multiple files.
- **Conventions**: project-specific conventions not already obvious from the code itself (e.g., from README.md, `.cursor/rules/`, or `.github/copilot-instructions.md` if those are added later).

Until then, treat any task in this repository as starting from a blank slate.
